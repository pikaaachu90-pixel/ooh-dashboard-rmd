import { useEffect,useState } from 'react'; import Layout from '../../components/Layout'; import { fetchData } from '../../lib/api';
export default function Revenue(){ const [dark,setDark]=useState(false); const [d,setD]=useState({rows:[],metrics:{}}); useEffect(()=>{fetchData().then(setD);},[]); const m=d.metrics||{}; return <Layout dark={dark} setDark={setDark}><h2 className='text-2xl font-bold mb-4'>Revenue & Cost Analytics</h2><div className='grid md:grid-cols-3 gap-3'><div className='card'>OOH Revenue: {m.revenue?.toFixed(0)}</div><div className='card'>Cost-to-Revenue: {(m.cost/m.revenue).toFixed(2)}</div><div className='card'>Margin %: {(((m.revenue-m.cost)/m.revenue)*100).toFixed(2)}</div></div></Layout>; }
