import Layout from '../../components/Layout'; import { useState } from 'react';
export default function Pipeline(){ const [dark,setDark]=useState(false); const steps=[['Data Sources','success'],['Cleaning','success'],['Transformation','success'],['Dashboard','success']]; return <Layout dark={dark} setDark={setDark}><h2 className='text-2xl font-bold mb-4'>Data Pipeline Visualization</h2><div className='grid md:grid-cols-4 gap-3'>{steps.map(([s,st])=><div key={s} className='card text-center'><p>{s}</p><span className='text-green-600'>{st}</span></div>)}</div><div className='card mt-4'>Flow: Sources → Cleaning → Transformation → Dashboard</div></Layout>; }
