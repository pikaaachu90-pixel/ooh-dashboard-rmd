import { useEffect,useState } from 'react'; import Layout from '../../components/Layout'; import { fetchData } from '../../lib/api';
export default function CustomerAnalytics(){ const [dark,setDark]=useState(false); const [rows,setRows]=useState([]); useEffect(()=>{fetchData().then(d=>setRows(d.rows));},[]); return <Layout dark={dark} setDark={setDark}><h2 className='text-2xl font-bold mb-4'>Customer Analytics</h2><div className='card'><table className='w-full text-sm'><thead><tr><th>Segment</th><th>Rooms/Beds/Employees/Students</th><th>Revenue</th></tr></thead><tbody>{rows.slice(0,25).map(r=><tr key={r.id}><td>{r.segment}</td><td>{r.customerAttributes.rooms||r.customerAttributes.beds||r.customerAttributes.employeeSize||r.customerAttributes.students||'-'}</td><td>{r.revenue}</td></tr>)}</tbody></table></div></Layout>; }
