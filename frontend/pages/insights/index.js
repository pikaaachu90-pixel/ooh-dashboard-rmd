import { useEffect,useState } from 'react'; import Layout from '../../components/Layout'; import { fetchInsights } from '../../lib/api';
export default function Insights(){ const [dark,setDark]=useState(false); const [d,setD]=useState({insights:[],anomalies:[],prediction:''}); useEffect(()=>{fetchInsights().then(setD);},[]); return <Layout dark={dark} setDark={setDark}><h2 className='text-2xl font-bold mb-4'>AI Insights Panel</h2><div className='card mb-3'>{d.insights.map(i=><p key={i}>• {i}</p>)}</div><div className='card mb-3'><b>Anomaly Detection</b>{d.anomalies.map(a=><p key={a.id}>{a.city} {a.segment} R/C {(a.revenue/a.cost).toFixed(2)}</p>)}</div><div className='card'><b>Predictive Insight</b><p>{d.prediction}</p></div></Layout>; }
